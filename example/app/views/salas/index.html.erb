<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style type="text/css">
* {
  box-sizing: border-box;
}

#myTable {
  border-collapse: collapse;
  width: 100%;
  border: 1px solid #000000;
  font-size: 18px;
}
#myTable th, #myTable td {
  text-align: left;
  padding: 12px;
}
#myTable tr {
  border-bottom: 1px solid #000000;
}

</style>
</head>
<body>
<br>
<h2 class="title is-2">Salas de chat</h2>

<table id="myTable">
  <tr class="header">
    <th style="width:30%;">Nombre sala</th>
    <th style="width:20%;">Link</th>
    <th style="width:30%;">Creada en</th>
    <th style="width:20%;">Mensajes</th>

  </tr>
  <% @salas.each do |sala| %>
  <tr>
    <td><%= sala.nombre %></td>
    <td><%= link_to 'Ver sala', sala, class: 'button is-link' %></td>
    <td><%= sala.created_at %></td>
    <td><%= Message.where(sala_id: sala.id).count %></td>
  </tr>
  <% end %>
</table>
<br>

<%= link_to 'Nueva Sala', new_sala_path, class: 'button is-success' %>
</body>
</html>